import { HistoryView } from '@/components/history/history-view';

export const metadata = {
  title: 'History — GitDoc AI',
  description: 'Track and review real activities, scans, and commits across your repositories.',
};

export default function HistoryPage() {
  return <HistoryView />;
}
